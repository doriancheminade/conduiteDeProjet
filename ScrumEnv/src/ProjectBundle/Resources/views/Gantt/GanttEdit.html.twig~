<html>
    <head>
        {% block stylesheets %}
	    <link href="{{ asset('css/twbs/bootstrap.css') }}" rel="stylesheet" />
	    <link href="{{ asset('css/ganttEdit.css') }}" rel="stylesheet" />
	    {% endblock %}
    </head>
    <body>
        {% block body %}
            {{ include('ProjectBundle::navbar.html.twig') }}
            <!-- TODO:draw the gantt chart here -->
            <div class="panel panel-default scrollable-panel">
            {% for task in taskList %}
                <div class="panel-body">
                {# include('ProjectBundle::Gantt/GanttEditTask.html.twig', { 'task': task }) #}
                {% render(controller('ProjectBundle:GAntt:editGAnttTaskAction',{ 'task': task })) %}
                </div>
            {% endfor %}
            </div>
        {% endblock %}
    </body>
</html>
