<html>
    <head>
        {% block stylesheets %}
	    <link href="{{ asset('bundles/testProject/css/main.css') }}" rel="stylesheet"  type="text/css"/>
	    <link href="{{ asset('css/twbs/bootstrap.css') }}" rel="stylesheet" />
	    {% endblock %}
    </head>
    <body>
        {% block body %}
            {{ include('ProjectBundle::navbar.html.twig') }}
            <h1>new sprint</h1>
            <form>
                {{ form_start(form) }}
                {{ form_errors(form) }}
                {{ dump(form) }}
                <div class="form-group">
                    <label for="description">{{ form_label(form.id) }}</label>    
			        {{ form_widget(form.id) }}
			    </div>
			    
                <div class="form-group">
                    <label for="description">{{ form_label(form.description) }}</label>    
			        {{ form_widget(form.description) }}
			    </div>
			    
			    <div class="form-group">
                    <label for="dateBeginging">{{ form_label(form.dateBegining) }}</label>
			        {{ form_widget(form.dateBegining) }}
			    </div>
			    
                <div class="form-group">
                    <label for="dateEnd">{{ form_label(form.dateEnd) }}</label>
			        {{ form_widget(form.dateEnd) }}
			    </div>
			    
                <input class="btn btn-success" type="submit" >
			    {# {{ form_label(form.create) }}
                {{ form_widget(form.create) }} #}
                {{ form_end(form) }}
            </form>
        {% endblock %}
    </body>
</html>
