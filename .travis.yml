language: php
php:
    - 5.5
  
before_script:
    - wget http://selenium.googlecode.com/files/selenium-server-standalone-2.39.0.jar
    - "nohup java -jar selenium-server-standalone-2.39.0.jar > /dev/null &"
    - composer install --no-interaction --working-dir ScrumEnv
    - cp  ScrumEnv/app/config/parameters.travis ScrumEnv/app/config/parameters.yml
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
    - php ScrumEnv/app/console doctrine:database:create -v --env=test
    - php ScrumEnv/app/console doctrine:schema:update -v --force --env=test
    - php ScrumEnv/app/console doctrine:fixtures:load -v --no-interaction --env=test
    - sleep 15
    - "php ScrumEnv/app/console server:run &"

script:
    - phpunit -c ScrumEnv/app/ -v
